<nav class="navbar navbar-expand-lg navbar-dark px-4" style="background: #1e1f22;">
  <a class="navbar-brand fw-bold" href="/"><i class="fas fa-robot me-2 text-primary"></i>BotPanel</a>
  <div class="ms-auto d-flex align-items-center gap-3">
    <% if (user) { %>
      <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" class="rounded-circle" width="32" height="32">
      <span class="text-white"><%= user.username %></span>
      <a href="/auth/logout" class="btn btn-sm btn-outline-secondary">Logout</a>
    <% } %>
  </div>
</nav>
